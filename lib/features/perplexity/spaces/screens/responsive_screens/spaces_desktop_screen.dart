import 'package:flutter/material.dart';

class SpacesDesktopScreen extends StatelessWidget {
  const SpacesDesktopScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Spaces Screen"),
    );
  }
}
