import 'package:flutter/material.dart';

class SpacesMobileScreen extends StatelessWidget {
  const SpacesMobileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
